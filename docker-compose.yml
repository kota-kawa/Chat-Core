services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: flask_app
    environment:
      - PYTHONUNBUFFERED=1
      - GROQ_API_KEY=${GROQ_API_KEY}
      - Gemini_API_KEY=${Gemini_API_KEY}
      - SEND_ADDRESS=${SEND_ADDRESS}
      - SEND_PASSWORD=${SEND_PASSWORD}
      - EMAIL_SEND_PASSWORD=${EMAIL_SEND_PASSWORD}
      - GROQ_MODEL=${GROQ_MODEL:-openai/gpt-oss-20b}
      - GEMINI_DEFAULT_MODEL=${GEMINI_DEFAULT_MODEL:-gemini-2.5-flash}
      - LLM_DAILY_API_LIMIT=${LLM_DAILY_API_LIMIT:-300}
      - AUTH_EMAIL_DAILY_SEND_LIMIT=${AUTH_EMAIL_DAILY_SEND_LIMIT:-50}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - PORT=5004
      - FRONTEND_URL=http://localhost:3000
      - POSTGRES_HOST=db
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      # FastAPI のホットリロード用（FLASK_ENV は SameSite/secure 切替に使用）
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    ports:
      - "5004:5004"
    volumes:
      - .:/app           # ホストのカレントディレクトリをコンテナ内 /app にマウント
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    image: node:20-alpine
    container_name: strike_frontend
    working_dir: /app/frontend
    environment:
      - BACKEND_URL=http://app:5004
    command: sh -c "npm install && npm run dev -- --hostname 0.0.0.0 --port 3000"
    volumes:
      - ./frontend:/app/frontend
      - frontend_node_modules:/app/frontend/node_modules
    ports:
      - "3000:3000"
    depends_on:
      - app

  db:
    image: postgres:15
    container_name: postgres_db
    environment:
      # 作成するデータベース
      POSTGRES_DB: ${POSTGRES_DB}
      # ここで作成するユーザ
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 5s

  redis:
    image: redis:7
    container_name: redis_cache
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 5s

volumes:
  db_data:
  frontend_node_modules:
